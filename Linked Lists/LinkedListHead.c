#include <stdio.h>
#include <stdlib.h>

typedef struct node {
    int data;
    struct node *next;
} elem;

elem *insertHead(elem *, int);
void view(elem *);
void maximum(elem *, int *, int *);                    // Passed through pointer

int main() {
    elem *List = NULL;
    int Result;
    
    
    
}
