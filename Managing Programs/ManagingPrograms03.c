/*
Dato il seguente tipo
struct list {
int val;
struct list *next;
};
Definire la funzione RICORSIVA
int liste_uguali(struct list * L1, struct list * L2)
seguente: la funzione deve restituire 1 se e solo se
le liste hanno lo stesso numero di nodi, e l'i-esimo
nodo della lista L1 contiene lo stesso valore (campo
val) dell'i-esimo nodo della lista L2.
*/
