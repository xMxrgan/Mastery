type input = {
    id: number;
    amount: number;
    description?: string;
    category: string;
};

enum categories {
    HEALTH = "🏥",
    TRAVEL = "✈️",
    EDUCATION = "📚",
    OTHER = "❓",
}

function App() {
    return;
}

export default App;
