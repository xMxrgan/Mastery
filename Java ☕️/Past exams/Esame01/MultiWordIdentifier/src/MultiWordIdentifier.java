public class MultiWordIdentifier {
    @Override
    public final String toString() {
        // TODO: si richiami il metodo ausiliario toString(pos, word) sulle componenti
        // e si concateni il risultato in un’unica stringa
        // restituisce la stringa con cui si stampa la componente pos-esima
        // dell’identificatore protected abstract String toString(int pos, String word);
        return "";
    }
}
